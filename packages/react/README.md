# @ascii-reveal/react

React bindings for AsciiReveal. The adapter delegates animation behavior to `@ascii-reveal/core`.

```tsx
import { AsciiReveal } from "@ascii-reveal/react";

<AsciiReveal text="BUILD SOMETHING MEMORABLE" />;
```
